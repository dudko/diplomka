import React from 'react';

const CompositeRotation = () => (
  <div>
    <h5>Orientation of components</h5>
    <input type="text" value={0} disabled={true} />
    <input type="text" value={0} disabled={true} />
    <input type="text" value={1} disabled={true} />
        <p></p>
  </div>
);

export default CompositeRotation;
